{
  "version": 2,
  "builds": [
    {
      "src": "apps/onlyconvert/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "apps/onlyconvert/dist" }
    }
  ],
  "routes": [
    { "src": "/robots.txt", "dest": "/robots.txt" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
